[package]
name = "meygned-core"
version = "0.1.0"
edition = "2021"
description = "Shared types, resolver flow, and errors for Meygned"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
